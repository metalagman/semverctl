version: "2"

run:
  tests: false

linters:
  default: fast
  disable:
    - wsl
    - depguard
    - wsl_v5
    - cyclop
    - funlen
    - nlreturn
    - gochecknoinits
    - lll
    - mnd
    - testpackage
    - nestif
